# Module 1: methods and conditionals

Pick **Java or Python**. Fill in the TODOs.

A plot can hold a flower only when it is empty (`0`) and both neighbors are empty. A missing neighbor at the end of the row counts as empty.

## Java

- `canPlant(prev, curr, next)`
- `canPlantAt(bed, i)`

```bash
javac Practice.java PracticeTest.java && java PracticeTest
```

## Python

- `can_plant(prev, curr, next)`
- `can_plant_at(bed, i)`

```bash
python3 practice.py
```

Course page: [Module 1](/resources/readiness-01-tracing)
