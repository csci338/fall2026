# Module 2: loops, counters, and accumulators

Pick **Java or Python**. Fill in the TODOs.

Walk the list or array once. Update a count as you go. For planting, mark a planted plot as `1` so the next index sees it.

## Java

- `countPlantable(bed)` — plant from the left, no two flowers next to each other, return how many you planted
- `countOccurrences(words, target)` — how many times `target` appears

```bash
javac Practice.java PracticeTest.java && java PracticeTest
```

## Python

- `count_plantable(bed)`
- `count_occurrences(words, target)`

```bash
python3 practice.py
```

Course page: [Module 2](/resources/readiness-02-methods-control-flow)
