# Module 3: lists and nested loops

Pick **Java or Python**. Fill in the TODOs.

Use two loops. The inner index must start at `i + 1` when you need two different positions.

## Java

- `targetSumNested(numbers, target)` — true if two different spots add to the target
- `countDistinctNested(numbers)` — how many different numbers (nested loops, not a set)

```bash
javac Practice.java PracticeTest.java && java PracticeTest
```

## Python

- `target_sum_nested(numbers, target)`
- `count_distinct_nested(numbers)`

```bash
python3 practice.py
```

Course page: [Module 3](/resources/readiness-03-lists-arrays)
