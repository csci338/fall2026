# Module 4: maps and grouping

Pick **Java or Python**. Fill in the TODOs.

Put each word into a list for a key. If the key is new, make a new list. Then append. Do not replace the list.

## Java

- `groupByFirstLetter(words)` — key is the first letter
- `groupByLength(words)` — key is how many characters

```bash
javac Practice.java PracticeTest.java && java PracticeTest
```

## Python

- `group_by_first_letter(words)`
- `group_by_length(words)`

```bash
python3 practice.py
```

Course page: [Module 4](/resources/readiness-04-maps-sets)
