# Module 5: algorithms and edge cases

Pick **Java or Python**. Fill in the TODOs.

Write two tests in your head first: one that should be true, one that should be false. Then code.

## Java

- `isAnagram(word1, word2)` — ignore spaces; same letters with the same counts
- `targetSumWithSet(numbers, target)` — use a set of numbers you have already seen; add each number *after* you check for `target - n`

```bash
javac Practice.java PracticeTest.java && java PracticeTest
```

## Python

- `is_anagram(word1, word2)`
- `target_sum_with_set(numbers, target)`

```bash
python3 practice.py
```

Course page: [Module 5](/resources/readiness-05-testing)
