# Diagnostic (Python)

Fill in the TODOs in `diagnostic.py`. Do not change the tests at the bottom.

## Functions

1. `group_by_first_letter` — group words by first letter
1. `target_sum` — True if two different spots add to the target
1. `count_distinct` — how many different numbers
1. `has_three_of_a_kind` — True if any card rank appears 3 or more times

The `Card` class is already in this file.

The full questions are on the [diagnostic page](/resources/programming-readiness-diagnostic).

## Run the tests

```bash
python3 diagnostic.py
```

You should see `Passed 15 tests.` when everything works.
